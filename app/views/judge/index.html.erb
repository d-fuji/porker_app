<section class="main">
    <h1 class="top">ポーカー役判別アプリ</h1>
    <section class="container">
        <%= form_tag("/") do %>
            <div class="form">
                <div class="form-body">
                    <input class="input" name="hands">
                    <input class="submit" type="submit" value="チェック">
                </div>
            </div>
        <% end %>
        <% @messages.each do |message| %>
            <p><%= message %>
        <% end %>
    </section>
</section>
